import 'package:movera_rider/core/maps/geo_point.dart';

/// Home still owns the destination UI. This holds extracted destination state.
class DestinationSession {
  String? address;
  GeoPoint? point;
}
